# Paramètres proxy communs
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host $host;
proxy_set_header X-Forwarded-Port $server_port;

# IMPORTANT : Laisser passer les cookies SANS modification
proxy_pass_header Set-Cookie;
proxy_set_header Cookie $http_cookie;

# NE PAS modifier les cookies - WordPress gère ça lui-même
# proxy_cookie_path / "/; Secure; HttpOnly; SameSite=Lax";  # COMMENTÉ

# Support WebSocket (si nécessaire)
proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $connection_upgrade;
proxy_cache_bypass $http_upgrade;
